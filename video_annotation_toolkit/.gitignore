# dependencies & build output
node_modules/
dist/
*.tsbuildinfo
.vite/

# Python venv for MediaPipe preprocessing (created by `npm run setup`, per-machine)
tools/.venv/
.venv/
__pycache__/

# local video data + generated skeletons (large; produced per-machine)
videos/
mediapipe_skeleton/

# per-video action labels (project data); keep the committed example template
action_labels/*
!action_labels/_example.json

# auto-saved annotation output (rep_counting.json + pose_analysis.json per video)
annotation/

# OMC state
.omc/

# misc
.DS_Store
*.local
*.log
